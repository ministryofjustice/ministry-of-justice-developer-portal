---
name: 🗂 Add New Issues to Project

on:
  issues:
    types:
      - opened

permissions: {}

jobs:
  call-reusable-workflow:
    permissions:
      contents: read
    uses: ministryofjustice/ministry-of-justice-engineering-platform/.github/workflows/reusable-add-issue-to-project.yml@17d8fd7272e3ce3ef756d709ae38f7309d16edee
    with:
      project-title: "👩‍💻 Developer Experience Team"
      project-fields-json: >-
        {"Status":"👇 To do","👏 Team":"💙 Blueprint","🤩 Refined?":"👎 Unrefined"}
    secrets:
      app-client-id: ${{ secrets.ISSUE_ORCHESTRATOR_GH_APP_CLIENT_ID }}
      app-private-key: ${{ secrets.ISSUE_ORCHESTRATOR_GH_APP_PRIVATE_KEY }}
      slack-webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
